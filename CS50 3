# include <cs50.h>
# include<stdio.h>

int main ()
{
	int num1, num2;
	int result;
	printf("Enter the num1:");
	scanf("%d",&num1);
	printf ("Enter the num2:");
	scanf("%d",&num2);
	result = num1+ num2;
	printf("The sum of %d is %d", num1,num2,result);

}
